<h1>All Beers</h1>
<table>
  <thead>
    <tr>
      <td>Beer Name</td>
      <td>Beer Type</td>
    </tr>
  </thead>
  <tbody>
    <% @beers.each do |beer| %>
      <tr class="beer-row" id="<%= dom_id(beer) %>">
        <td class="name"><%= beer.name %></td>
        <td class="beer_type"><%= beer.beer_type %></td>
        <td><%= link_to 'Edit', edit_beer_path(beer) %></td></td>
      </tr>
    <% end %>
  </tbody>
</table>
